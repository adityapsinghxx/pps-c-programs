#include <stdio.h>

int main()
{
    int a = 5, b = 2;
    float result;

    result = (float)a / b; //manual conversion of one data type into another//
    printf("Result = %f", result);

    return 0;
}
